
// imaudio.spawn.speaker

{
	init(Spawn) {

	},
	dispatch(event) {
		let APP = imaudio,
			Self = APP.spawn.speaker,
			el;
		switch (event.type) {
			// custom events
			case "some-event":
				break;
		}
	}
}
